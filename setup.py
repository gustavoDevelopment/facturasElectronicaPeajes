from setuptools import setup, find_packages

setup(
    name="facturae_optimus",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # Lista de dependencias del proyecto
    ],
    python_requires='>=3.6',
)
